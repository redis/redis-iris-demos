name: risks to risk JSON
source:
  schema: public
  table: risks
output:
  - uses: redis.write
    with:
      connection: target
      data_type: json
      on_update: merge
      key:
        expression: concat(['risk:', risk_id])
        language: jmespath

name: people
source:
  schema: public
  table: people
output:
  - uses: redis.write
    with:
      connection: target
      data_type: json
      on_update: merge
      key:
        expression: concat(['person:', person_id])
        language: jmespath
